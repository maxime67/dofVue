pipeline {
    agent any

    environment {
        // Repository and deployment variables
        ANSIBLE_REPO = 'https://github.com/maxime67/dofVue'
        ANSIBLE_INVENTORY = 'ansible/inventory/hosts.yml'
    }

    stages {
        stage('Checkout Ansible') {
            steps {
                git branch: 'master',
                    url: ANSIBLE_REPO
            }
        }

        stage('Run Ansible Playbook') {
            steps {
                sh """
                set -x
                    ANSIBLE_HOST_KEY_CHECKING=False ansible-playbook -i ${ANSIBLE_INVENTORY} ansible/site.yml \
                        -vvv
                """
            }
        }
    }

    post {
        always {
            cleanWs()
        }
        success {
            echo 'Deployment completed successfully!'
        }
        failure {
            echo 'Deployment failed!'
        }
    }
}
